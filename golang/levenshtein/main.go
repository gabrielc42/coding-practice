package main

func main() {
	// Levenshtein distance: a string metric for measuring difference between two sequences
	// calculated by preparing a matrix of size (M+1)x(N+1)
	// —where M and N are the lengths of the 2 words—and looping through said matrix using 2 for loops,
	// performing some calculations within each iteration.
}
